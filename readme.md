```
// format
$ terraform fmt
// preview
$ terraform plan
// apply
$ terraform apply
// destroy
$ terraform destroy
// validate
$ terraform validate
// graph
$ terraform graph | dot -Tpng > graph.png
// get module
$ terraform get
// indicate variable value
$ terraform plan -var 'environment=dev'
```
